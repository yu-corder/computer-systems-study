// This file is part of www.nand2tetris.org
// and the book "The Elements of Computing Systems"
// by Nisan and Schocken, MIT Press.
// File name: projects/3/b/RAM512.hdl
/**
 * Memory of 512 16-bit registers.
 * If load is asserted, the value of the register selected by
 * address is set to in; Otherwise, the value does not change.
 * The value of the selected register is emitted by out.
 */
CHIP RAM512 {
    IN in[16], load, address[9];
    OUT out[16];

    PARTS:
    //// Replace this comment with your code.

    DMux8Way(in=load, sel=address[6..8], a=loadA, b=loadB, c=loadC, d=loadD, e=loadE, f=loadF, g=loadG, h=loadH);

    RAM64(in=in, address=address[0..5], load=loadA, out=r0);
    RAM64(in=in, address=address[0..5], load=loadB, out=r1);
    RAM64(in=in, address=address[0..5], load=loadC, out=r2);
    RAM64(in=in, address=address[0..5], load=loadD, out=r3);
    RAM64(in=in, address=address[0..5], load=loadE, out=r4);
    RAM64(in=in, address=address[0..5], load=loadF, out=r5);
    RAM64(in=in, address=address[0..5], load=loadG, out=r6);
    RAM64(in=in, address=address[0..5], load=loadH, out=r7);

    Mux8Way16(a=r0, b=r1, c=r2, d=r3, e=r4, f=r5, g=r6, h=r7, sel=address[6..8], out=out);
}